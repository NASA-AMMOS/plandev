table:
  name: directive_source_is_activity
  schema: merlin
configuration:
  custom_name: "directive_source_is_activity"
object_relationships:
  - name: plan
    using:
      foreign_key_constraint_on:
        - scheduled_plan_id
  - name: scheduled_directive
    using:
      manual_configuration:
        column_mapping:
          scheduled_directive_id: id
          scheduled_plan_id: plan_id
        remote_table:
          name: activity_directive
          schema: merlin
  - name: referenced_directive
    using:
      manual_configuration:
        column_mapping:
          referenced_directive_id: id
          scheduled_plan_id: plan_id
        remote_table:
          name: activity_directive
          schema: merlin
select_permissions:
  - role: aerie_admin
    permission:
      columns: '*'
      filter: {}
      allow_aggregations: true
  - role: user
    permission:
      columns: '*'
      filter: {}
      allow_aggregations: true
  - role: viewer
    permission:
      columns: '*'
      filter: {}
      allow_aggregations: true
insert_permissions:
  - role: aerie_admin
    permission:
      columns: '*'
      check: {}
  - role: user
    permission:
      columns: '*'
      check: {}
delete_permissions:
  - role: aerie_admin
    permission:
      filter: {}
